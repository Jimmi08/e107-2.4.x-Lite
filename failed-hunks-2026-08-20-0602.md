# Failed hunks — sync round 4

**None. This file is empty by outcome, not by omission.**

Every hunk that had a target in Lite applied cleanly with `git apply` (no
`--3way`, no `--force`, no hand-resolution). One hunk applied with a line
offset, which is a normal relocation and not a failure:

- `1fe177f56` → `ehandlers/news_class.php`: *"Hunk #1 succeeded at 544 (offset 1
  line)."* Pre-image matched exactly; Lite simply sits one line further down than
  upstream in that file.

Two commits produced `git apply` warnings of the form
`ehandlers/admin_ui.php has type 100644, expected 100755`
(`65d87b4d6`, `a81acde03`). That is a file-mode difference between Lite and
upstream, not a hunk failure — the content applied in full and the mode was left
as Lite has it.

No `.rej` or `.orig` files were produced, and none exist in the tree.

Hunks that were *not applied* in this run were all skipped for lack of a target
(no `eplugins/featurebox/` in Lite, and `e107_tests/` is never shipped), not
because they failed to apply. Those are itemised in
`report-sync-round4-2026-08-20-0602.md`.
